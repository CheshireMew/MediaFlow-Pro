import QtQuick
import QtQuick.Controls
import ".."

MenuSeparator {
    padding: 2
    verticalPadding: 7
    contentItem: Rectangle {
        implicitWidth: 208
        implicitHeight: 1
        color: Theme.borderStrong
    }
}
